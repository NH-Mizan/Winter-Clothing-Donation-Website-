import React from 'react';
import { useLoaderData } from 'react-router-dom';

const HowToHelp = () => {

    
    return (
        <div>
            {/* <section className="bg-gray-100 py-12">
                <div className="max-w-7xl mx-auto px-6">
                    <h2 className="text-3xl font-bold text-center mb-8">কিভাবে কাজ করে</h2>
                    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
                        {steps &&
                        steps.map((step, index) => (
                            <div
                                key={index}
                                className="card bg-white shadow-lg p-6 text-center border-t-4 border-blue-500 hover:shadow-xl transition-transform transform hover:scale-105"
                            >
                                <div className="text-5xl mb-4">{step.icon}</div>
                                <h3 className="text-xl font-semibold mb-2">{step.title}</h3>
                                <p className="text-gray-600">{step.description}</p>
                            </div>
                        ))}
                    </div>
                </div>
            </section> */}
        </div>
    );
};

export default HowToHelp;